apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base

replicas:
- count: 3
  name: vote
namespace: staging

nameSuffix: -staging

labels:
- includeSelectors: false
  pairs:
    project: instavote
#patches:
#- path: service.yaml

